import { TimelineTimeDirective } from './timeline-time.directive';

describe('TimelineTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineTimeDirective();
    expect(directive).toBeTruthy();
  });
});
