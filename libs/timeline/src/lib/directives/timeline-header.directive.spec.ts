import { TimelineHeaderDirective } from './timeline-header.directive';

describe('TimelineHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
