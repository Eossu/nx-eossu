import { MagnetDirective } from './magnet.directive';

describe('MagnetDirective', () => {
  it('should create an instance', () => {
    const directive = new MagnetDirective();
    expect(directive).toBeTruthy();
  });
});
