import { VertexDirective } from './vertex.directive';

describe('VertexDirective', () => {
  it('should create an instance', () => {
    const directive = new VertexDirective();
    expect(directive).toBeTruthy();
  });
});
